# Test targets

These are real yet fake crates/workspace to use as test targets.
They are not meant to represent idiomatic or meaningful Rust code,
just showcase Rust features to use in manual/integration testing.

Benchmark-specific targets live in `bench_fixtures/`. Some are generated stress
targets, while others are small realistic workspaces used to compare analysis
behavior across different project shapes.
